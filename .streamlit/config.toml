[theme]
base = "light"
primaryColor = "#5a6c7d"           # Muted accent (clinical, used sparingly)
backgroundColor = "#FFFFFF"        # Cold paper white (not cream) - UNIFIED
secondaryBackgroundColor = "#FFFFFF"  # CHANGED: Same as background for unification
textColor = "#2B2B2B"              # Charcoal text (high readability)
font = "sans serif"                # Changed to sans-serif to match body text
